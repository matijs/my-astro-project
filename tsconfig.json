{
  "$schema": "https://json.schemastore.org/tsconfig",
  "$comment": "Only present to enable TypeScript support",
  "extends": "astro/tsconfigs/strictest",
  "includes": [".astro/types.d.ts", "**/*"],
  "excludes": ["./dist/"],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@components/*": ["./src/components/*"],
      "@layouts/*": ["./src/layouts/*"]
    }
  }
}
